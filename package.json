{
  "name": "kuiland",
  "version": "1.0.0",
  "description": "Studio kui — Infraestructura digital para negocios que se mueven",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "serve": "node server.js 8000",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
